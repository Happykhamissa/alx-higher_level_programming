#!/usr/bin/python3

def raise_exception_msg(message=""):
    """
    Raises a name exception with a custom message.

    Args:
        message (str, optional): The custom message for the exception.
    """
    raise NameError(message)
